function desastre() {
  // 1. Indentación horrible (4 espacios en vez de 2)
  const mensaje = "Hola mundo"; // 2. Comillas simples (deberían ser dobles)
    
  // 3. Variable que no cambia (debería ser const)
  const numero = 10; 
    
  // 4. Falta punto y coma arriba ^ y aquí abajo
  console.log(mensaje);

  // 5. Comparación débil (== en vez de ===)
  if (numero === 10) {
    console.log("El número es diez");
  }
}
